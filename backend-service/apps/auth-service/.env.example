# Local Nest startup defaults to http://127.0.0.1:8200 when VAULT_ADDR is
# omitted. Docker Compose supplies http://vault:8200 explicitly.
VAULT_ADDR=http://127.0.0.1:8200
AUTH_SERVICE_VAULT_ROLE_ID=
AUTH_SERVICE_VAULT_SECRET_ID=
PORT=3001
ALLOW_MULTI_SESSION=false
