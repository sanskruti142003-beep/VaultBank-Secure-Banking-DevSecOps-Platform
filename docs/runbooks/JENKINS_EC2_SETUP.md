# Jenkins EC2 Setup

Use the current first-ten-phases setup guide:

```text
docs/JENKINS_EC2_SETUP.md
```

That guide uses the required Jenkins credential IDs:

```text
sonarqube-token
nvd-api-key
harbor-robot
harbor-ca-cert
```

Older ECR, Argo CD, ZAP, DefectDojo, and k3s deployment notes are for later phases and are not part of the current Jenkins phases 1-10 pipeline.
