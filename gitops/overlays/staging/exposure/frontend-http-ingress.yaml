apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vaultbank-frontend-http
  namespace: vault-bank-staging
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: vaultbank
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: vault-bank-staging-vaultbank-https-redirect@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: staging.vaultbank.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 8080
